# Futures contract

Record contract month, roll rule, settlement versus trade price, expiry, margin, tick size, collateral yield, and whether a return is spot, front contract, or synthetic continuous-roll return. Roll yield must be separated from spot movement.
